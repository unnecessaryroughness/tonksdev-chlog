TODO:
======

BUGS
    * (none)

USERS
    * Remove User

UI
    * UI basic framework 
        + Common mobile stylesheet
        + Common intermediate stylesheet
        + Common large-screen stylesheet
        + Common logged in / redirect 

    * UI improvements
        + Style Common header
        + Style Common footer
        + Style Common menu
    
SECURITY
    * replace hashing mechanism:
    
        function register($username, $password) {
            $hash = password_hash($password, PASSWORD_BCRYPT);
            save($user, $hash);
        }

        function login($username, $password) {
            $hash = loadHashByUsername($username);
            if (password_verify($password, $hash)) {
                //login
            } else {
                // failure
            }
        }

    * build function to verify a password by retrieving hash for a user
      and using the password_verify() function
      
HOSTING
    * Move CHLOG to chlog.tonksdev.co.uk
    
    
    